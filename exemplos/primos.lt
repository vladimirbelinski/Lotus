let n, flag = 0 : int;

println(Digite o número para saber se é primo);
scan(n);


for(let i = 2: int; i <= n^1/2; i = i + 1){
	if(n%i == 0){
		flag = 1;
	}
}

if (!flag){
	println(O número $n$ é primo);
}
else {
	println(O número $n$ não é primo);
}
