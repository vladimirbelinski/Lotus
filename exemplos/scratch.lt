let x = 0, len = 0: int;
let s = "Lotus": string;

if (x == 0) {
	len = s;
	while (x < len) {
		println([$s^x$]);
		x = x + 1;
	}
}

println($s$);
println($x$);
x = x + s; -- concatena o a string no int, não soma com o tamanho!
println($x$);
