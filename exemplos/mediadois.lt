-- Exemplo de programa que calcula a média aritmética entre dois números utilizando a Linguagem Lotus

let x, y: double;

println(Digite o primeiro número:);
scan(x);
println(Digite o segundo número:);
scan(y);

println(A média dos dois números é: $(x+y)/2$);
